import { TNotificationObject } from "./types";
export declare const Notification: {
    ({ icon, title, message, buttonAction, actionText, }: TNotificationObject): import("react").JSX.Element;
    displayName: string;
};
