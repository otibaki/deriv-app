import React from "react";
import { TScore } from "./PasswordConstants";
import "./PasswordMeter.scss";
type PasswordMeterProps = {
    score: TScore;
};
export declare const PasswordMeter: ({ score }: PasswordMeterProps) => React.JSX.Element;
export {};
