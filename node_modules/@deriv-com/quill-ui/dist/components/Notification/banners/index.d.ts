import { default as React } from 'react';
import { NotificationBannerProps } from '../banner';
export interface NotificationBannersProps {
    autohideTimeout?: number;
    className?: string;
    banners?: Array<NotificationBannerProps & {
        id: string;
    }>;
    isMobile?: boolean;
    onClick?: (bannerId: string) => void;
    onClose?: (bannerId: string) => void;
    zIndex?: number;
}
export declare const NotificationBanners: ({ autohideTimeout, banners, className, isMobile, onClick, onClose, zIndex, }: NotificationBannersProps) => React.JSX.Element | null;
export default NotificationBanners;
