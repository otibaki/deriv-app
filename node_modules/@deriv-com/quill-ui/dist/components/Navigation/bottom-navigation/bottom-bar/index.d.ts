import { default as React } from 'react';
export interface BottomBarProps extends React.ComponentProps<React.ElementType> {
    showLabels?: boolean;
    as?: React.ElementType;
    value?: number;
    onChange?: (event: React.ChangeEvent<HTMLElement>, value: number) => void;
}
declare const BottomBar: {
    ({ as: Element, className, children, onChange, value, showLabels, ...rest }: BottomBarProps): React.JSX.Element;
    displayName: string;
};
export default BottomBar;
