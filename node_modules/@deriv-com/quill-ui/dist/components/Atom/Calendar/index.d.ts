import { default as Calendar } from 'react-calendar';
export interface DatePickerProps extends Omit<React.ComponentProps<typeof Calendar>, "onChange" | "activeStartDate" | "maxDetail" | "minDetail" | "defaultView"> {
    hasFixedWidth?: boolean;
    optionsConfig?: Record<string, string>;
    onChange?: (value: Value, event?: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;
    onFormattedDate?: (value: string) => void;
    startDate?: Date;
    wrapperClassName?: string;
    disableCurrentDayMarker?: boolean;
}
type ValuePiece = Date | null;
type Value = ValuePiece | [ValuePiece, ValuePiece];
export declare const DatePicker: ({ startDate, allowPartialRange, calendarType, className, defaultValue, hasFixedWidth, formatMonthYear, goToRangeStartOnSelect, locale, navigationLabel, next2Label, nextLabel, onChange, onFormattedDate, prev2Label, prevLabel, returnValue, selectRange, showNavigation, showNeighboringMonth, tileClassName, value, wrapperClassName, disableCurrentDayMarker, ...rest }: DatePickerProps) => import("react").JSX.Element;
export default DatePicker;
