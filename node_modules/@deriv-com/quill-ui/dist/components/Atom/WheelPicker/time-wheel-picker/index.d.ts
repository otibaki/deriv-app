import { default as React } from 'react';
import { TimeWheelPickerContainerProps } from '../types';
export declare const TimeWheelPickerContainer: ({ is12Hour, startTimeIn24Format, endTimeIn24Format, selectedTime, minutesInterval, hoursInterval, locale, setSelectedValue, setSelectedTime, close, data, disabled, containerHeight, }: TimeWheelPickerContainerProps) => React.JSX.Element;
export default TimeWheelPickerContainer;
