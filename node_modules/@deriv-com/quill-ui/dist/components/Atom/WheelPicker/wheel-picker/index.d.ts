import { default as React } from 'react';
import { WheelPickerContainerProps } from '../types';
export declare const WheelPickerContainer: ({ data, inputValues, setInputValues, close, setSelectedValue, disabled, containerHeight, }: WheelPickerContainerProps) => React.JSX.Element;
export default WheelPickerContainer;
