import { TMediumSizes } from '../../../../types.ts';
import { default as React, ElementType, HTMLAttributes, ReactNode } from 'react';
export interface DropdownTitleProps extends HTMLAttributes<HTMLElement> {
    as?: ElementType;
    size?: TMediumSizes;
    centered?: boolean;
    label: ReactNode;
    icon?: ReactNode;
}
export declare const DropdownTitle: React.ForwardRefExoticComponent<DropdownTitleProps & React.RefAttributes<HTMLElement>>;
