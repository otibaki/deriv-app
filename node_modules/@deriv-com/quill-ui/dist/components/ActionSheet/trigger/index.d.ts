import { IconSize } from '@deriv/quill-icons';
import { QuillIconComponent } from '../../../types.ts';
import { ButtonProps } from '../../Button';
interface ButtonTriggerProps extends Omit<ButtonProps, "ref"> {
    iconComponent?: never;
    iconSize?: never;
    iconClassName?: never;
}
type IconTriggerProps = {
    iconComponent?: QuillIconComponent;
    className?: string;
    iconSize?: IconSize;
    iconClassName?: string;
};
type TriggerProps = ButtonTriggerProps | IconTriggerProps;
declare const Trigger: {
    ({ iconComponent: Icon, iconClassName, ...restProps }: TriggerProps): import("react").JSX.Element;
    displayName: string;
};
export default Trigger;
