import { ComponentPropsWithoutRef, ReactNode } from 'react';
export interface HeaderProps extends Omit<ComponentPropsWithoutRef<"div">, "title"> {
    title?: ReactNode;
    description?: ReactNode;
    icon?: ReactNode;
    iconPosition?: "right" | "left";
    closeIcon?: ReactNode;
    centered?: boolean;
}
declare const Header: ({ className, title, description, icon: Icon, iconPosition, closeIcon: CloseIcon, centered, ...rest }: HeaderProps) => import("react").JSX.Element;
export default Header;
