import { MutableRefObject } from 'react';
import { TabProps } from '../types';
export type TabContextType = TabProps & {
    handleToggle?: (idx: number) => void;
    activeTab?: number;
    clickedTabRef?: MutableRefObject<HTMLButtonElement | null> | null;
};
export declare const TabContext: import('react').Context<TabContextType>;
export declare const TabContainer: ({ children, id, size, iconPosition, contentStyle, className, selectedTabIndex, onChangeTab, onTabClick, }: TabProps) => import("react").JSX.Element;
