import { default as React } from 'react';
export interface SwitchToggleProps {
    disabled?: boolean;
    defaultChecked?: boolean;
    onChange?: (isEnabled: boolean) => void;
    className?: string;
    id?: string;
    onFocus?: () => void;
    onBlur?: () => void;
    checked?: boolean;
}
export declare const ToggleSwitch: ({ disabled, onChange, className, defaultChecked, onFocus, onBlur, id, checked, }: SwitchToggleProps) => React.JSX.Element;
export default ToggleSwitch;
