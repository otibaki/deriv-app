import { ComponentProps, PropsWithChildren } from 'react';
import { TMediumSizes, TLeftOrRight } from '../../../../types.ts';
export interface IRadio extends Omit<ComponentProps<"input">, "placeholder" | "size" | "ref"> {
    className?: string;
    classNameInfo?: string;
    classNameLabel?: string;
    hasInfo?: boolean;
    onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void;
    size?: TMediumSizes;
    radioButtonPosition?: TLeftOrRight;
}
export declare const RadioButton: ({ children, className, classNameInfo, classNameLabel, defaultChecked, disabled, hasInfo, id, onChange, size, value, radioButtonPosition, ...otherProps }: PropsWithChildren<IRadio>) => import("react").JSX.Element;
export default RadioButton;
