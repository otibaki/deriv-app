import { CheckboxProps } from '../checkbox-single';
interface Node extends Omit<CheckboxProps, "id" | "children" | "ref"> {
    id: string | number;
}
interface ParentNode extends Node {
    children?: Node[];
}
interface CheckboxGroupProps {
    className?: string;
    checkboxGroupConfig: ParentNode[];
}
export declare const CheckboxGroup: ({ className, checkboxGroupConfig, }: CheckboxGroupProps) => import("react").JSX.Element;
export {};
