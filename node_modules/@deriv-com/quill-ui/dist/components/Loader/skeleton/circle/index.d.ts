import { default as React } from 'react';
import { SkeletonBaseProps } from '../base';
export interface CircleProps extends SkeletonBaseProps {
    width?: number | string;
}
declare const Circle: {
    ({ width, className, style, ...rest }: CircleProps): React.JSX.Element;
    displayName: string;
};
export default Circle;
