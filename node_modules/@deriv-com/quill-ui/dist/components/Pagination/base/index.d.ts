import { PaginationProps } from '../types';
import { default as React } from 'react';
export interface PaginationButtonProps extends Pick<PaginationProps, "variant"> {
    pageNumber: number | string;
    currentPage: number;
    handleOnClick: (event: string | null) => void;
}
export declare const PaginationButton: ({ currentPage, handleOnClick, pageNumber, variant, }: PaginationButtonProps) => React.JSX.Element;
export default PaginationButton;
