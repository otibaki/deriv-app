import { default as React, ReactNode } from 'react';
import { TRegularSizes } from '../../types.ts';
export interface Option {
    disabled?: boolean;
    icon?: ReactNode;
    label?: ReactNode;
    selected?: boolean;
}
export interface SegmentedControlProps {
    className?: string;
    hasAnimation?: boolean;
    options: Array<Option>;
    onChange?: (selectedItemIndex: number) => void;
    size?: TRegularSizes;
}
export declare const SegmentedControl: ({ className, hasAnimation, options, onChange, size, }: SegmentedControlProps) => React.JSX.Element;
