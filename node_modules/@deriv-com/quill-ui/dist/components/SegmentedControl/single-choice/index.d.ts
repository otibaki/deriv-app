import { default as React } from 'react';
import { Option, SegmentedControlProps } from '../base';
export interface SegmentedControlSingleChoiceProps extends SegmentedControlProps {
    hasContainerWidth?: boolean;
    options: Array<Omit<Option, "selected">>;
    selectedItemIndex?: number;
}
export declare const SegmentedControlSingleChoice: ({ className, options, onChange, hasContainerWidth, selectedItemIndex, size, }: SegmentedControlSingleChoiceProps) => React.JSX.Element | null;
export default SegmentedControlSingleChoice;
