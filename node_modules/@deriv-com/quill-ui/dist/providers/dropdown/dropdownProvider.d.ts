import { default as React, ReactNode } from 'react';
export interface TDropdownProvider {
    children: ReactNode;
    onOpen?: () => void;
    onClose?: () => void;
}
export declare const DropdownProvider: ({ children, onOpen, onClose, }: TDropdownProvider) => React.JSX.Element;
